# {{.Name}} ({{.Type}})
{{ .Description | trimspace }}

{{ if .HasExample -}}
## Example Usage
{{ tffile .ExampleFile }}
{{- end }}

## Schema changes
### v7.10
bgp - fixed ```as_override``` and rename to ```output {as_override}```
bgp - fixed ```remove_private_as``` and rename to ```output {remove_private_as}```


{{ .SchemaMarkdown | trimspace }}

{{ if .HasImport -}}
## Import
Import is supported using the following syntax:
{{ codefile "shell" .ImportFile }}
{{- end }}
